"""
Logic extracted from ``TradingBotOrchestrator`` (thin delegates live in ``trading_bot.py``).

Submodules are intentionally flat named ``*_runtime`` for grep-friendly discovery — import symbols
explicitly from each module, e.g. ``from trading.bot_runtime.websocket_runtime import …``.
"""
